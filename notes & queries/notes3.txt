 views are some query that we give a name to and we can store so we do not have to write the query over and over 

 view acts as virtual table

 having is used with group by to put a condition 

 with rollup returns a new row by giving total count etc.

 Window functions perform aggregate operations on groups of rows, but they produce a result FOR EACH ROW.

 a tool called Faker that allows you to create fake placeholder data to use in your applications. 